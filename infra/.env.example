# KardiaFlow Infrastructure Configuration
# Sourced by automation scripts (deploy, publish, teardown)

# REQUIRED
SUB="<your-subscription-id>"          # Azure subscription ID
RG="kardia-rg-dev"                    # Main resource group name
DEPLOY="kardiaflow"                   # Bicep deployment name (used to read outputs)
ADLS="kardiaadlsdemo"                 # ADLS Gen2 account (globally unique, lowercase 3–24 chars)
WORKSPACE="kardia-dbx"                # Databricks workspace name
SCOPE="kardia"                        # Databricks secret scope used by kflow
DATABRICKS_PAT="dapi<your-token>"     # Databricks PAT (from User Settings → Developer)

# OPTIONAL (safe defaults exist; override if you like)
CONT="lake"                           # ADLS container used by notebooks/config
MANAGED_RG=""                         # If set, teardown will use this instead of auto-discovery
APP_NAME="kardiaflow-sp"              # Display name for the Service Principal (ensure_sp.sh)

# Notes:
# - DATABRICKS_HOST is set automatically by the scripts; you don’t need to define it here.